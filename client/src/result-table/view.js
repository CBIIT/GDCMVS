
let tmpl = '<div class="container table__container">'
  +'<div class="table__thead row">'
  +'<div class="col-xs-4"><div class="table__th">Name</div></div>'
  +'<div class="col-xs-4"><div class="table__th">Description</div></div>'
  +'<div class="col-xs-4"><div class="table__th table__th--right">'
    +'<div class="checkbox checkbox-th"><label class="checkbox__label">'
      +'<input class="checkbox__input" id="trs-checkbox" type="checkbox" value="">'
      +'<span class="checkbox__btn"><i class="checkbox__icon fa fa-check"></i></span> Show all values'
    +'</label></div>'
    +'<button type="button" id="tree_toggle" class="btn btn-th" data-toggle="button" aria-pressed="false" autocomplete="off">'
      +'<i class="btn-th__icon fa fa-angle-down"></i> Expand All'
    +'</button>'
  +'</div></div>'
+'</div>'
+'<div class="table__body table__body--overflow row" style="max-height: {{:mh}}px;">'
  +'<table class="treetable table" id="tree_table">'
    +'<tbody>'
    +'{{for trs}}'
    +'<tr key="{{:id}}" data-tt-id="{{:data_tt_id}}" data-tt-parent-id="{{:data_tt_parent_id}}" class="data-table-row {{:node}} {{if exist != true && type == "value"}}data-hide hide{{/if}}">'
    +'<td width="33%">'
    +'<span class="{{:type}} title table__td--word-break" style="display:inline-block; width: 70%;">'
    +'{{if type == "folder"}}'
    +'<a href="https://docs.gdc.cancer.gov/Data_Dictionary/viewer/#?view=table-definition-view&id={{:l_id}}" target="_blank">{{:title}} {{if len !== undefined}}({{:len}}){{/if}}</a>'
    +'{{else type == "link"}}'
      +'{{if l_type == "cde"}}'
      +'No values in GDC, reference values in <a href="javascript:getCDEData(\'{{:l_id}}\');" class="table-td-link">caDSR</a>'
      +'{{else}}'
      +'No values in GDC, concept referenced in <a target="_blank" href="{{:url}}" class="table-td-link">NCIt</a>'
      +'{{/if}}'
          +'{{else}}'
          +'{{:title}} {{if len && len !== 0}}({{:len}}){{/if}}'
          +'{{/if}}'
          +'</td>'
          +'<td width="66%">'
          +'{{:desc}}'
          +'</td>'
          +'</tr>'
    +'{{/for}}'
    +'</tbody>'
  +'</table>'
+'</div>'
+'</div>';

export default tmpl;
